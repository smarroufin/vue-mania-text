{
  "name": "docs",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "vue-tsc --noEmit && nuxi typecheck",
    "postinstall": "nuxt prepare",
    "predev": "(cd .. && pnpm run build)",
    "prebuild": "(cd .. && pnpm run build)",
    "pregenerate": "(cd .. && pnpm run build)"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.12.1",
    "nuxt": "3.13.2",
    "tailwindcss": "^3.4.13"
  },
  "devDependencies": {
    "@nuxt/eslint": "^0.5.7"
  }
}
